feat: sistema de reapprovisionnement - fournisseurs, commandes et template email

- Schema: suppliers renomme contact->contact_name, ajout address/notes/updated_at; nouvelles tables purchase_orders et purchase_order_items; colonnes email_template_* dans company_settings
- Routers tRPC: suppliers (CRUD + bulk assign), purchase-orders (create, list, getById, updateStatus, getCriticalArticles, saveEmailTemplate)
- Generation de reference CMD-YYYYMMDD-XXX atomique via pg_advisory_xact_lock (sans race condition)
- Helper countCriticalArticles partage entre dashboard et purchaseOrders (sans duplication)
- Pages: /restock (onglets Articles critiques + Modele email), /settings/suppliers, /purchase-orders
- Dashboard: card Articles critiques cliquable vers /restock
- Sidebar: liens Reapprovisionnement et Commandes
- Formulaire article: select fournisseur actif; action en masse Associer fournisseur
